"# Testing Strategy & Implementation Plan\n\nWe need to verify the core systems of our SQLite-backed offline-first React Native Expo app. The strategy is divided into two parts: Jest unit/integration tests for components and hooks under the mocked NodeJS runtime, and E2E automation tests using Maestro YAML driving actual user paths on the mobile client.\n\n## Proposed Test Suites\n\n### 1. Jest Unit & Integration Tests\n\nWe will create detailed unit tests using Jest, focusing on hooks, pages, and components.\n\n#### [NEW] [useDatabaseSync.test.tsx](file:///c:/Users/blued/gits/llm-pdf-ingest/mobile/src/hooks/__tests__/useDatabaseSync.test.tsx)\n- Verifies that the SQLite custom hook `useDatabaseSync` retrieves corpora and documents on mount.\n- Confirms that sections are correctly loaded dynamically for an active document.\n- Verifies that using the hook outside of `DatabaseProvider` throws a descriptive error.\n- Validates exception handling when database calls fail.\n\n#### [NEW] [LibraryScreen.test.tsx](file:///c:/Users/blued/gits/llm-pdf-ingest/mobile/app/__tests__/index.test.tsx)\n- Tests the pre-ingestion Library Dashboard screen.\n- Verifies rendering of document lists (pulled from hook).\n- Validates uploading triggers and LLM route selections (`local`, `network`, `cloud` routing).\n- Simulates database inserts and atomic transaction rollbacks on failure.\n\n#### [NEW] [ReadingScreen.test.tsx](file:///c:/Users/blued/gits/llm-pdf-ingest/mobile/app/reader/__tests__/[id].test.tsx)\n- Tests the multi-pane responsive reader.\n- Mocks dynamic search parameters (`id`).\n- Verifies section swapping and incremental block loading.\n- Tests creating, updating, and deleting annotations (highlights + notes) using the BottomSheet/NoteEditor depending on device profiles.\n\n---\n\n### 2. Maestro E2E Integration Testing\n\nMaestro YAML files will automate E2E user paths, driving actual screen transitions and validations.\n\n#### [NEW] [import_document.yaml](file:///c:/Users/blued/gits/llm-pdf-ingest/mobile/maestro/import
<truncated 1101 bytes>