"# Codebase vs. Specs Meticulous Review & Implementation Plan\n\nBased on a meticulous review of the `mobile/` and `rust_core/` folders against the specifications in `docs/` (`specs.md`, `impl.md`, `mobile_frontend.md`, `database_sync.md`), several significant inconsistencies and unfinished mock components have been identified. \n\nTo achieve a production-ready application, we must transition the React Native codebase from hardcoded mocks into the robust, SQLite-backed, multi-pane architecture defined in the specs.\n\n## User Review Required & Open Questions\n\n> [!WARNING]\n> Please review the following inconsistencies and provide guidance on how you would like to proceed.\n\n1. **Routing & Navigation Architecture**: `App.tsx` currently directly renders `<ReadingScreen />`. The specs (`impl.md` & `mobile_frontend.md`) demand a `NavigationProvider` splitting into `TabletView` (3-pane) and `SmartphoneView` (Drawer + Bottom Sheet). Shall we implement `react-navigation` (e.g., `@react-navigation/native` and `@react-navigation/drawer`) to handle this, or build a custom layout switcher using `expo-router`?\n2. **Pre-Ingestion Dashboard**: The specs require a dashboard displaying inference speeds and routing choices (Local vs. Network vs. Cloud). Currently, `ReadingScreen.tsx` mocks this with a simple `Alert.alert`. Should this dashboard be a dedicated screen (`LibraryScreen.tsx`) or a full-screen modal prior to entering the reader?\n3. **Database Schema Redundancy**: `schema.ts` contains both an `embeddings` table (`vector TEXT`) and a `vector_cache` table (`vector BLOB`). `impl.md` Phase 12 explicitly specifies using `vector_cache` for binary `Float32Array` embeddings. Can I safely delete the `embeddings` table to prevent duplication?\n4. **Mocked SQLite Data vs Real DB Queries**: `ReadingScreen.tsx` is entirely driven by `useState` mocks (e.g., `setDocuments`, `setSectionBlocksCache`). Moving to production means rewriting these to execute asynchronous SQLite queries (`db.getAllAsync()`, etc.) and using the memory ch
<truncated 2058 bytes>