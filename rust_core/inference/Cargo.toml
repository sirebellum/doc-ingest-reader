[package]
name = "inference"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
# llama_cpp dependencies or dynamic linking configurations will go here
