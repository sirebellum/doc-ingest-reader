"# Implementation Plan - Phase 8: On-Device RAG Assistant, Interactive SVG Concept Graph, and Local-Network Mesh Sync Rooms\n\nThis plan covers the design, file structures, and verification protocols for implementing a local Retrieval-Augmented Generation (RAG) assistant with scroll-aligned badged citation maps, a premium interactive SVG knowledge graph mapping concepts and documents with smooth micro-animations, and an automated ZeroConf Wi-Fi multi-peer synchronization mesh room.\n\n## Goal Description\n\nEnhance the LLM PDF reader application with advanced research and collaborative reading capabilities:\n1. **Local RAG Assistant**: Run offline Q&A using local hybrid FTS5 and semantic vector-hashing search to assemble the context, query the local model natively via `RustParserBridge.runInferenceAsync`, and parse/render inline scrollable citation links.\n2. **Interactive SVG Knowledge Graph**: Render a beautiful, premium visual node-link concept web using `react-native-svg` and `react-native-reanimated` with interactive node navigation and block drawers.\n3. **ZeroConf Wi-Fi Collaboration Rooms**: Discover and pair devices on the local Wi-Fi subnet automatically, establish bidirectional WebSocket sync streams, and broadcast real-time annotation deltas that overlay instantly without app reloads.\n\n---\n\n## User Review Required\n\n> [!IMPORTANT]\n> **SVG Force-Directed Layout Simulation**:\n> We will implement a high-efficiency spring-mass force layout algorithm in React Native JS. It computes node positions on component mount and stabilizes them quickly using a damped velocity integrator, avoiding background performance bottlenecks while maintaining premium 60fps micro-animations.\n\n> [!NOTE]\n> **Network WebSocket Servers in Expo**:\n> Because native TCP/WebSocket listening servers are not supported out-of-the-box by basic Expo standard client sandboxes without building separate native target dependencies, we will build a `ZeroConfMeshService` that uses client WebSocket protocols to connect to configured/dis
<truncated 5037 bytes>